---
/**
 * French catalogue (`/machines/`).
 *
 * Every line of markup lives in CataloguePage.astro, shared with the English
 * route, so the two catalogues cannot drift apart.
 */
import CataloguePage from "@/components/CataloguePage.astro";
---

<CataloguePage locale="fr" />
