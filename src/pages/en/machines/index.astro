---
/**
 * English catalogue (`/en/machines/`).
 *
 * Same template as the French route — only the locale differs.
 */
import CataloguePage from "@/components/CataloguePage.astro";
---

<CataloguePage locale="en" />
